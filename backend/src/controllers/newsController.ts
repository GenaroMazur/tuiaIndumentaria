import { NextFunction, Request, Response } from "express";


export const newsList =async (req:Request, res:Response, next:NextFunction) => {
    try {
        
    } catch (error) {
        console.error(error)
        next(error)
    }
}

export const newsDetail =async (req:Request, res:Response, next:NextFunction) => {
    try {
        
    } catch (error) {
        console.error(error)
        next(error)
    }
}

export const newsCreate =async (req:Request, res:Response, next:NextFunction) => {
    try {
        
    } catch (error) {
        console.error(error)
        next(error)
    }
}
export const newsUpdate =async (req:Request, res:Response, next:NextFunction) => {
    try {
        
    } catch (error) {
        console.error(error)
        next(error)
    }
}
export const newsDelete =async (req:Request, res:Response, next:NextFunction) => {
    try {
        
    } catch (error) {
        console.error(error)
        next(error)
    }
}